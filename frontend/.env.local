# Base URL for the Django API (include /api if using the default router)
VITE_API_BASE_URL=http://127.0.0.1:8000/api

# OAuth client ID from Google Cloud Console (Web client, must match frontend origin)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com